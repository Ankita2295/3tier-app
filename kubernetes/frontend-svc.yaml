apiVersion: v1
kind: Service
metadata:
  name: react-nginx-service
spec:
  type: NodePort
  selector:
    app: react-nginx-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
