apiVersion: v1
kind: Service
metadata:
  name: node-backend-service
spec:
  type: NodePort
  selector:
    app: node-backend-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 3001
      nodePort: 30008
